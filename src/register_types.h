#pragma once

#include <godot_cpp/core/class_db.hpp>

void desktop_capture_initialize(godot::ModuleInitializationLevel p_level);
void desktop_capture_terminate(godot::ModuleInitializationLevel p_level);
